# Julio Alvarez — Personal Landing Page

Landing personal construida con Astro y pensada para alojarse como sitio estático en GitHub Pages.

## Desarrollo local

```bash
npm install
npm run dev
```

## Compilar

```bash
npm run build
```

GitHub Actions compila el contenido de `dist/` y lo despliega automáticamente en GitHub Pages al hacer push a `main`.
